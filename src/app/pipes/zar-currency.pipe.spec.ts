import { ZarCurrencyPipe } from './zar-currency.pipe';

describe('ZarCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ZarCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
